<Page x:Class="FurnitureManagement.Client.Views.SaleOrderManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FurnitureManagement.Client.Views"
      xmlns:converters="clr-namespace:FurnitureManagement.Client.Converters"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="销售订单管理"
      Loaded="Page_Loaded">
    <Page.Resources>
        <!-- 销售订单状态转换器 -->
        <converters:SaleStatusConverter x:Key="SaleStatusConverter"/>
    </Page.Resources>
    <Grid Background="#F5F5F5">
        <StackPanel Margin="20">
            <!-- 标题和操作按钮 -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="销售订单管理" FontSize="24" FontWeight="Bold" Foreground="#333333"/>
                <Button Grid.Column="1" Content="添加订单" Width="100" Height="35" Background="#2ECC71" Foreground="White" 
                        BorderBrush="Transparent" Click="btnAddOrder_Click" HorizontalAlignment="Right">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#27AE60"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
            
            <!-- 筛选和搜索区域 -->
            <Border Background="White" Padding="20" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <Grid Grid.Column="0" Grid.Row="0" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Text="日期范围" FontSize="14" FontWeight="Medium" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="#333333"/>
                        <DatePicker x:Name="dpStartDate" Width="Auto" Height="45" Padding="10" VerticalContentAlignment="Center" Grid.Column="1" Margin="0,0,10,0">
                            <DatePicker.Style>
                                <Style TargetType="DatePicker">
                                    <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="BorderBrush" Value="#3498DB"/>
                                        </Trigger>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter Property="BorderBrush" Value="#3498DB"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DatePicker.Style>
                        </DatePicker>
                        <TextBlock Text="至" FontSize="14" FontWeight="Medium" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="#333333" Grid.Column="2"/>
                        <DatePicker x:Name="dpEndDate" Width="Auto" Height="45" Padding="10" VerticalContentAlignment="Center" Grid.Column="3">
                            <DatePicker.Style>
                                <Style TargetType="DatePicker">
                                    <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="BorderBrush" Value="#3498DB"/>
                                        </Trigger>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter Property="BorderBrush" Value="#3498DB"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DatePicker.Style>
                        </DatePicker>
                    </Grid>
                    
                    <Grid Grid.Column="0" Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBox x:Name="txtSearch" Width="Auto" Height="35" Padding="10" VerticalContentAlignment="Center">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="BorderBrush" Value="#3498DB"/>
                                        </Trigger>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter Property="BorderBrush" Value="#3498DB"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                        <ComboBox x:Name="cmbStatus" Width="Auto" Height="35" Padding="10" VerticalContentAlignment="Center" Grid.Column="1" Margin="10,0,0,0">
                            <ComboBox.Style>
                                <Style TargetType="ComboBox">
                                    <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="BorderBrush" Value="#3498DB"/>
                                        </Trigger>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter Property="BorderBrush" Value="#3498DB"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ComboBox.Style>
                            <ComboBoxItem Content="全部" Tag=""/>
                            <ComboBoxItem Content="待处理" Tag="pending"/>
                            <ComboBoxItem Content="已完成" Tag="completed"/>
                            <ComboBoxItem Content="已取消" Tag="cancelled"/>
                        </ComboBox>
                    </Grid>
                    
                    <Button Content="搜索" Width="80" Height="35" Background="#3498DB" Foreground="White" 
                            BorderBrush="Transparent" Click="btnSearch_Click" Grid.Column="1" Margin="10,0,0,0" Grid.Row="1" VerticalAlignment="Center">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#2980B9"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    
                    <Button Content="重置" Width="80" Height="35" Background="#BDC3C7" Foreground="White" 
                            BorderBrush="Transparent" Click="btnReset_Click" Grid.Column="2" Margin="10,0,0,0" Grid.Row="1" VerticalAlignment="Center">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#95A5A6"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
            
            <!-- 销售订单列表 -->
            <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8">
                <DataGrid x:Name="dgSaleOrders" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" 
                          SelectionMode="Single" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
                    <!-- 行样式 -->
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Height" Value="45"/>
                            <Setter Property="Background" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F8F9FA"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#E6F2FF"/>
                                    <Setter Property="Foreground" Value="#3498DB"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                    
                    <!-- 单元格样式 -->
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderBrush" Value="#E0E0E0"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="Padding" Value="10"/>
                        </Style>
                    </DataGrid.CellStyle>
                    
                    <!-- 列定义 -->
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="订单ID" Binding="{Binding SaleId}" Width="80" IsReadOnly="True"/>
                        <DataGridTextColumn Header="客户姓名" Binding="{Binding CustomerName}" Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="客户电话" Binding="{Binding CustomerPhone}" Width="120" IsReadOnly="True"/>
                        <DataGridTextColumn Header="销售日期" Binding="{Binding SaleDate, StringFormat='yyyy-MM-dd'}" Width="110" IsReadOnly="True"/>
                        <DataGridTextColumn Header="总金额" Binding="{Binding TotalAmount, StringFormat='¥{0:N2}'}" Width="90" IsReadOnly="True"/>
                        <DataGridTextColumn Header="折扣" Binding="{Binding Discount, StringFormat='¥{0:N2}'}" Width="80" IsReadOnly="True"/>
                        <DataGridTextColumn Header="最终金额" Binding="{Binding FinalAmount, StringFormat='¥{0:N2}'}" Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="状态" Binding="{Binding Status, Converter={StaticResource SaleStatusConverter}}" Width="80" IsReadOnly="True">
                            <DataGridTextColumn.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="pending">
                                            <Setter Property="Foreground" Value="#F39C12"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="completed">
                                            <Setter Property="Foreground" Value="#27AE60"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="cancelled">
                                            <Setter Property="Foreground" Value="#E74C3C"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.CellStyle>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Header="操作" Width="150" IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="查看" Width="60" Height="28" Background="#3498DB" Foreground="White" 
                                                BorderBrush="Transparent" Click="btnViewOrder_Click" Margin="0,0,5,0" Tag="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#2980B9"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                        <Button Content="编辑" Width="60" Height="28" Background="#F39C12" Foreground="White" 
                                                BorderBrush="Transparent" Click="btnEditOrder_Click" Tag="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#E67E22"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </StackPanel>
    </Grid>
</Page>