<Window x:Class="FurnitureManagement.Client.Views.PurchaseDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:FurnitureManagement.Client.Views"
        mc:Ignorable="d"
        d:DesignHeight="600"
        d:DesignWidth="800"
        Title="采购订单明细"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5">
    <Grid>
        <StackPanel Margin="20">
            <!-- 标题 -->
            <TextBlock Text="采购订单明细" FontSize="24" FontWeight="Bold" Margin="0,0,0,30" HorizontalAlignment="Center" Foreground="#333333"/>

            <!-- 订单基本信息 -->
            <Border Background="White" Padding="20" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5" Margin="0,0,0,20">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <StackPanel Margin="0,0,40,0">
                            <TextBlock Text="订单ID" FontSize="14" Foreground="#666666" Margin="0,0,0,5"/>
                            <TextBlock x:Name="txtOrderId" FontSize="16" FontWeight="Bold" Foreground="#333333"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,40,0">
                            <TextBlock Text="供应商" FontSize="14" Foreground="#666666" Margin="0,0,0,5"/>
                            <TextBlock x:Name="txtSupplier" FontSize="16" FontWeight="Bold" Foreground="#333333"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,40,0">
                            <TextBlock Text="采购日期" FontSize="14" Foreground="#666666" Margin="0,0,0,5"/>
                            <TextBlock x:Name="txtPurchaseDate" FontSize="16" FontWeight="Bold" Foreground="#333333"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,40,0">
                            <TextBlock Text="订单状态" FontSize="14" Foreground="#666666" Margin="0,0,0,5"/>
                            <TextBlock x:Name="txtStatus" FontSize="16" FontWeight="Bold" Foreground="#333333"/>
                        </StackPanel>
                        <StackPanel>
                            <TextBlock Text="总金额" FontSize="14" Foreground="#666666" Margin="0,0,0,5"/>
                            <TextBlock x:Name="txtTotalAmount" FontSize="16" FontWeight="Bold" Foreground="#3498DB"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- 采购明细列表 -->
            <Border Background="White" Padding="20" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="采购明细" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

                    <DataGrid x:Name="dgPurchaseDetails" AutoGenerateColumns="False" FontSize="14" CanUserAddRows="False" SelectionMode="Single" SelectionUnit="FullRow"
                              Background="White" BorderBrush="#E0E0E0" BorderThickness="1" RowHeight="45" AlternatingRowBackground="#F9F9F9">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F5F5F5"/>
                                <Setter Property="Foreground" Value="#333333"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="Padding" Value="10,12"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.Columns>
                            <!-- 修复绑定：使用正确的属性路径 -->
                            <DataGridTextColumn Header="商品名称" Width="200" Binding="{Binding Furniture.FurnitureName}" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Left"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Padding" Value="10,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="仓库" Width="150" Binding="{Binding Warehouse.WarehouseName}" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="数量" Width="80" Binding="{Binding Quantity, StringFormat=N0}" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="单价" Width="100" Binding="{Binding UnitPrice, StringFormat=C}" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Padding" Value="0,0,10,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="金额" Width="100" Binding="{Binding Amount, StringFormat=C}" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Padding" Value="0,0,10,0"/>
                                        <Setter Property="Foreground" Value="#3498DB"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

            <!-- 底部按钮 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button x:Name="btnClose" Content="关闭" FontSize="14" Padding="20,12" Width="150" Background="#409EFF" Foreground="White"
                        BorderBrush="Transparent" BorderThickness="0" Click="btnClose_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#66B1FF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>