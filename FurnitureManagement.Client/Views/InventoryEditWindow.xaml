<Window x:Class="FurnitureManagement.Client.Views.InventoryEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FurnitureManagement.Client.Views"
        mc:Ignorable="d"
        Title="库存编辑"
        Width="550"
        Height="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip">
    <Grid Background="#F5F5F5">
        <StackPanel Margin="20">
            <!-- 标题 -->
            <TextBlock x:Name="txtTitle" Text="添加库存" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" Foreground="#333333"/>
            
            <!-- 表单区域 -->
            <Border Background="White" Padding="20" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8">
                <StackPanel>
                    <!-- 商品选择 -->
                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="商品 *" FontSize="14" FontWeight="Medium" Margin="0,0,0,5" Foreground="#333333"/>
                        <ComboBox x:Name="cmbFurniture" Width="Auto" Height="35" Padding="10" VerticalContentAlignment="Center" SelectedValuePath="FurnitureId" DisplayMemberPath="FurnitureName">
                            <ComboBox.Style>
                                <Style TargetType="ComboBox">
                                    <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="BorderBrush" Value="#3498DB"/>
                                        </Trigger>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter Property="BorderBrush" Value="#3498DB"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ComboBox.Style>
                        </ComboBox>
                    </StackPanel>
                    
                    <!-- 仓库选择 -->
                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="仓库 *" FontSize="14" FontWeight="Medium" Margin="0,0,0,5" Foreground="#333333"/>
                        <ComboBox x:Name="cmbWarehouse" Width="Auto" Height="35" Padding="10" VerticalContentAlignment="Center" SelectedValuePath="WarehouseId" DisplayMemberPath="WarehouseName">
                            <ComboBox.Style>
                                <Style TargetType="ComboBox">
                                    <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="BorderBrush" Value="#3498DB"/>
                                        </Trigger>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter Property="BorderBrush" Value="#3498DB"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ComboBox.Style>
                        </ComboBox>
                    </StackPanel>
                    
                    <!-- 数量 -->
                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="数量 *" FontSize="14" FontWeight="Medium" Margin="0,0,0,5" Foreground="#333333"/>
                        <TextBox x:Name="txtQuantity" Width="Auto" Height="35" Padding="10" VerticalContentAlignment="Center" PreviewTextInput="txtQuantity_PreviewTextInput">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="BorderBrush" Value="#3498DB"/>
                                        </Trigger>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter Property="BorderBrush" Value="#3498DB"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                    </StackPanel>
                    
                    <!-- 错误信息 -->
                    <TextBlock x:Name="txtError" FontSize="12" Foreground="#E74C3C" Margin="0,0,0,15" Visibility="Collapsed"/>
                    
                    <!-- 按钮区域 -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="取消" Width="80" Height="35" Margin="0,0,10,0" Background="#E0E0E0" Foreground="#333333" 
                                BorderBrush="Transparent" Click="btnCancel_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#BDBDBD"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button x:Name="btnSave" Content="保存" Width="80" Height="35" Background="#3498DB" Foreground="White" 
                                BorderBrush="Transparent" Click="btnSave_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#2980B9"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Window>