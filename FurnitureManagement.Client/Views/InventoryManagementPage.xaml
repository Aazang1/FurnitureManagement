<Page x:Class="FurnitureManagement.Client.Views.InventoryManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FurnitureManagement.Client.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="库存管理">
    <Grid Background="#F5F5F5">
        <StackPanel Margin="20">
            <!-- 标题和操作按钮 -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="库存管理" FontSize="24" FontWeight="Bold" Foreground="#333333"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBox x:Name="txtSearch" Width="250" Height="35" Margin="0,0,10,0" 
                             VerticalContentAlignment="Center" Padding="10" Visibility="Collapsed">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="BorderBrush" Value="#3498DB"/>
                                    </Trigger>
                                    <Trigger Property="IsFocused" Value="True">
                                        <Setter Property="BorderBrush" Value="#3498DB"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    <Button Content="搜索" Width="80" Height="35" Background="#3498DB" Foreground="White" 
                            BorderBrush="Transparent" Visibility="Collapsed">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#2980B9"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Content="添加库存" Width="100" Height="35" Background="#2ECC71" Foreground="White" 
                            BorderBrush="Transparent" Click="btnAddInventory_Click" Margin="10,0,0,0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#27AE60"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
            
            <!-- 库存列表 -->
            <DataGrid x:Name="dgInventory" AutoGenerateColumns="False" CanUserAddRows="False" 
                      SelectionMode="Single" SelectionUnit="FullRow" Background="White"
                      BorderBrush="#E0E0E0" BorderThickness="1" RowHeight="40">
                <!-- 列样式 -->
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F5F5F5"/>
                        <Setter Property="Foreground" Value="#333333"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="Padding" Value="10"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                
                <!-- 行样式 -->
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E6F2FF"/>
                                <Setter Property="Foreground" Value="#3498DB"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                
                <!-- 单元格样式 -->
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="Padding" Value="10"/>
                    </Style>
                </DataGrid.CellStyle>
                
                <!-- 列定义 -->
                <DataGrid.Columns>
                    <DataGridTextColumn Header="库存ID" Binding="{Binding InventoryId}" Width="90" IsReadOnly="True"/>
                    <DataGridTextColumn Header="商品名称" Binding="{Binding Furniture.FurnitureName}" Width="160" IsReadOnly="True"/>
                    <DataGridTextColumn Header="仓库" Binding="{Binding Warehouse.WarehouseName}" Width="160" IsReadOnly="True"/>
                    <DataGridTextColumn Header="数量" Binding="{Binding Quantity}" Width="110" IsReadOnly="True">
                        <DataGridTextColumn.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Quantity}" Value="0">
                                        <Setter Property="Foreground" Value="#E74C3C"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.CellStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="最后更新" Binding="{Binding LastUpdated, StringFormat='yyyy-MM-dd HH:mm'}" Width="180" IsReadOnly="True"/>
                    <DataGridTemplateColumn Header="操作" Width="220" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <!-- 编辑按钮 -->
                                    <Button Content="编辑" Width="60" Height="28" Background="#F39C12" Foreground="White" 
                                            BorderBrush="Transparent" Click="btnEditInventory_Click" Tag="{Binding}" Margin="0,0,5,0">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#E67E22"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                    <!-- 删除按钮 -->
                                    <Button Content="删除" Width="60" Height="28" Background="#E74C3C" Foreground="White" 
                                            BorderBrush="Transparent" Click="btnDeleteInventory_Click" Tag="{Binding}" Margin="0,0,5,0">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#C0392B"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                    <!-- 调整数量按钮 -->
                                    <Button Content="调整数量" Width="80" Height="28" Background="#3498DB" Foreground="White" 
                                            BorderBrush="Transparent" Click="btnAdjustQuantity_Click" Tag="{Binding}">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#2980B9"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            
            <!-- 分页控件 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <!-- 每页显示条数选择 -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                    <TextBlock Text="每页显示:" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="#333333"/>
                    <ComboBox x:Name="cmbPageSize" Width="80" Height="30" VerticalContentAlignment="Center" SelectedIndex="1">
                        <ComboBoxItem Content="5条"/>
                        <ComboBoxItem Content="10条"/>
                        <ComboBoxItem Content="20条"/>
                        <ComboBox.Style>
                            <Style TargetType="ComboBox">
                                <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="BorderBrush" Value="#3498DB"/>
                                    </Trigger>
                                    <Trigger Property="IsFocused" Value="True">
                                        <Setter Property="BorderBrush" Value="#3498DB"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ComboBox.Style>
                    </ComboBox>
                </StackPanel>
                
                <!-- 分页导航按钮 -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                    <Button x:Name="btnFirstPage" Content="首页" Width="50" Height="30" Background="#95A5A6" Foreground="White" 
                            BorderBrush="Transparent" Click="btnFirstPage_Click" Margin="0,0,5,0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#7F8C8D"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button x:Name="btnPrevPage" Content="上一页" Width="60" Height="30" Background="#95A5A6" Foreground="White" 
                            BorderBrush="Transparent" Click="btnPrevPage_Click" Margin="0,0,5,0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#7F8C8D"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <TextBlock x:Name="txtPageInfo" Text="第1页/共1页" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="#333333"/>
                    <Button x:Name="btnNextPage" Content="下一页" Width="60" Height="30" Background="#95A5A6" Foreground="White" 
                            BorderBrush="Transparent" Click="btnNextPage_Click" Margin="0,0,5,0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#7F8C8D"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button x:Name="btnLastPage" Content="末页" Width="50" Height="30" Background="#95A5A6" Foreground="White" 
                            BorderBrush="Transparent" Click="btnLastPage_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#7F8C8D"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
                
                <!-- 总记录数显示 -->
                <TextBlock x:Name="txtTotalInfo" Text="共0条记录" VerticalAlignment="Center" Foreground="#333333"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>